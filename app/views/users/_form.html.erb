<%= form_for :user do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.password_field :password %>
    <%= f.submit(@user.new_record? ? "Sign Up" : "Sign In", class: "text-white") %>
<% end %>